package Chapter_7_practice_set;

public class ptr_arthematic {
    public static void main(String[] args) {
        int arr[] = new int[3];

        System.out.println(arr);
        // arr = arr + 1;
        // arr++
        /*
        Java does not support pointer arithmetic or incrementing references (arr = arr + 1 is invalid)

         */
        //only arr[i] is possible which is like *(arr + 1)
        // in java we cannot acess address so reference and dereference is not possible
        
    }
}
